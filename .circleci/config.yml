# unused - remove file once circleCI integration is removed
version: 2.1

jobs:
  do_nothing:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout

workflows:
  do_not_trigger:
    jobs:
      - do_nothing:
          filters:
            branches:
              ignore: /.*/
